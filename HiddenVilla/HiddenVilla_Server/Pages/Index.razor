@page "/"

@*<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />*@

@* RAZOR *@
@*This is one way data binding. Client cannot change attributes on objects, id, price etc*@
@*ASP.NET Core Blazor documentation*@

<h2 class="bg-light border p-2">
    First Room
</h2>
Room: @Room.RoomName
<br/>
Price: @Room.Price
<br/>

@*&nbsp; = syntax for space*@
<input type="number" @bind-value="Room.Price" @bind-value:event="oninput" />
<br/>
<input type="checkbox" @bind-value="Room.IsActive" checked="@(Room.IsActive?"checked":null)" /> &nbsp; Is Active
<br/>
<span>This room is @(Room.IsActive?"Active":"Inactive")</span><br/>

<select @bind="SelectedRoomProp">
    @foreach (var prop in Room.RoomProps)
    {
        <option value="@prop.Name">@prop.Name</option>
    }

</select>
<span>The Room Prop selected is: @SelectedRoomProp</span>

@code{
    // C SHARP
    string SelectedRoomProp = "";

    Model.BlazorRoom Room = new Model.BlazorRoom()
        {
            Id = 1,
            RoomName = "Villa Suite",
            IsActive = true,
            Price = 499,
            RoomProps = new List<Model.BlazorRoomProp>
            {
                new Model.BlazorRoomProp{Id=1, Name="Sq Ft", Value="100"},
                new Model.BlazorRoomProp{Id=2, Name="Occupancy", Value="3"},
            }
        };
}
